# ===========================================
# Caddy Configuration
# ===========================================

# Email used for ACME certificate registration when MOS_TLS_MODE=cloudflare-dns01.
CADDY_ACME_EMAIL=

# Scoped Cloudflare API token used by Caddy for DNS-01 certificate issuance and renewal.
# Required only when MOS_TLS_MODE=cloudflare-dns01.
CLOUDFLARE_API_TOKEN=
