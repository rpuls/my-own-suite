# ===========================================
# OnlyOffice Configuration
# ===========================================
# Copy this file to .env and configure
# DOMAIN is loaded from root .env
# ===========================================

# Time zone used by the container
TZ=Europe/Amsterdam

# Allow callbacks/downloads to private or loopback addresses.
# Required for local development (localhost) and many private-network deployments.
ALLOW_PRIVATE_IP_ADDRESS=true

# Allow callbacks/downloads to metadata/non-public cloud address ranges.
# Usually not needed on local VPS deploys.
ALLOW_META_IP_ADDRESS=false

# Force a stable useraddress value for the built-in /example app.
# This prevents file lookup mismatches behind multi-proxy ingress chains.
ONLYOFFICE_EXAMPLE_USERADDRESS=127.0.0.1

# JWT configuration.
# Keep disabled until you integrate with a document provider (e.g. Seafile).
JWT_ENABLED=false

# Set a long random secret before enabling JWT.
# Generate with: openssl rand -hex 32
JWT_SECRET=
