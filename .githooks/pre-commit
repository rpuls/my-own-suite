#!/bin/sh

branch="$(git symbolic-ref --short HEAD 2>/dev/null || true)"

if [ "$branch" = "main" ]; then
  echo "ERROR: committing on 'main' is blocked."
  echo "Create a branch first, e.g.: git checkout -b docs/my-change"
  echo "Then commit from that branch and open a PR."
  exit 1
fi

exit 0
