---
title: Getting Started
description: Run My Own Suite locally with Docker Compose.
slug: docs/getting-started
---

Use this to run the full stack locally on your machine.

## Prerequisites

- Docker Desktop installed and running
- Node.js 20+ and npm
- Git

## 1. Clone the repo

```bash
git clone https://github.com/rpuls/my-own-suite.git
cd my-own-suite
```

## 2. Initialize env files

```bash
npm run vps:init
```

This creates missing `.env` files from `deploy/vps/**/*.env.template` without overwriting existing values.

## 3. Optional customization

`vps:init` auto-generates required secrets from template expressions in `*.env.template`.

The main first-run values live in `deploy/vps/services/suite-manager/.env`:

- `OWNER_NAME`
- `OWNER_EMAIL`
- `OWNER_PASSWORD`
- `TIMEZONE`
- `BOOTSTRAP_TOKEN`

You can also change `DOMAIN` in `deploy/vps/.env` and adjust individual service env files in `deploy/vps/services/*.env` if needed.

## 4. Run environment checks

```bash
npm run vps:doctor
```

This validates required variables and cross-file wiring (including the Radicale token used by Homepage).

## 5. Build and run the stack

```bash
npm run vps:up
```

Use this instead of `vps:rebuild` for normal startup.

If you need a full destructive reset (removes Docker volumes/data):

```bash
npm run vps:rebuild
```

## 6. Finish onboarding

- Authelia sign-in: `https://auth.mos.localhost/`
- Suite Manager: `https://suite-manager.mos.localhost/`

Sign in through Authelia first, then open `Suite Manager` and complete the onboarding flow before you start using the rest of the suite.

Homepage also includes a `Finish Setup` tile that points to the same onboarding flow.

## 7. Open the apps

- Homepage: `https://homepage.mos.localhost/`

From Homepage you can open each application. Direct URLs:

- Seafile: `http://seafile.mos.localhost/`
- ONLYOFFICE: `http://onlyoffice.mos.localhost/`
- Immich: `http://immich.mos.localhost/`
- Radicale: `http://radicale.mos.localhost/`
- Stirling PDF: `http://stirling-pdf.mos.localhost/`
- Vaultwarden: `https://vaultwarden.mos.localhost/`
- Suite Manager: `https://suite-manager.mos.localhost/`
