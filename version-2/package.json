{
  "name": "my-own-suite-version-2",
  "private": true,
  "description": "Clean-slate MOS V2 launch platform workspace.",
  "type": "commonjs",
  "scripts": {
    "branding:sync": "node scripts/sync-branding.cjs",
    "test": "npm run branding:sync && node --test suite-manager/backend/test/*.test.cjs test/*.test.cjs"
  },
  "engines": {
    "node": "22.x",
    "npm": "11.x"
  }
}
